/**
 * CK AWS Plugin.
 *
 * <p>The plugin owns exactly two concerns: AWS authentication (STS AssumeRole
 * and credential lifecycle) and generic, AWS-service-agnostic AWS CLI
 * execution. See CLAUDE.md for the architecture.
 */
package io.github.rads4.ckaws;
